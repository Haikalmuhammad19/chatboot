[build]
pythonVersion = "3.11"

[start]
cmd = "cd backend/backend-chatbot-lstm-main && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
