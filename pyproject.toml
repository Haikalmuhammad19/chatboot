[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chatbot-lstm-backend"
version = "1.0.0"
description = "Chatbot LSTM Backend API"
requires-python = ">=3.11"
